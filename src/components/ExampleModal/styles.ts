import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  scrollView: {
    marginTop: 18,
    flexGrow: 1,
    paddingBottom: 45,
    alignItems: "center",
  },
})

export default styles
