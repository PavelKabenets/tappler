import { SCREEN_HEIGHT } from "helpers/helpers"
import { StyleSheet } from "react-native"

export const scrollHeightStep2 = SCREEN_HEIGHT / 4.5

export default StyleSheet.create({
  scrollView: {
    flexGrow: 1,
  },
})
